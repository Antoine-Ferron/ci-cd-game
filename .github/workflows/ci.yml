name: CI Workflow

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: build
        run: cargo build
      - name: run
        run: cargo run
      
